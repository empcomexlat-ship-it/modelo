<div class="partials_contenedor_encabezado">
    @if (!empty($titulo))
        <h2>{!! $titulo !!}</h2>
    @endif

    @if (!empty($descripcion))
        <p>{!! $descripcion !!}</p>
    @endif
</div>
